# Copy this file to .env and fill in values.
# If MONGO_URI is left blank/unset, the server automatically falls back
# to local JSON file storage in server/data/ — no database required.
MONGO_URI=
PORT=5000
